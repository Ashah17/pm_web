Individual imports (keep track of any new ones needed below):
** be sure to install these in a virtual env for isolation + to make sure you're there's no python version inconsistencies **

pip install flask
pip install flask_cors
pip install serpapi
pip install requests
pip install beautifulsoup4
pip install praw
pip install langchain-google-genai
pip install flask
pip install flask_cors
pip install langchain_community
pip install langchain
pip install langchain_huggingface
pip install chromadb

OR do it in one line:

pip install flask flask_cors serpapi requests beautifulsoup4 praw langchain-google-genai flask flask_cors langchain_community langchain langchain_huggingface chromadb google-search-results